{
  "name": "",
  "version": "2.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "install:server": "cd server && npm install",
    "install:client": "cd client && npm install",
    "install:all": "npm install && npm run install:server && npm run install:server",
    "start:server": "cd server && npm run start:nodemon",
    "start:client": "cd client && npm start",
    "build:client": "cd client && npm run build",
    "build:server": "cd server && npm run build",
    "compile": "node compile.js",
    "compile:server": "npm run build:server && node compile.js",
    "compile:client": "npm run build:client && node compile.js",
    "build": "npm run build:client && npm run build:server && npm run compile"
  },
  "author": "Zach Moore",
  "license": "ISC",
  "dependencies": {
    "fs-jetpack": "^2.2.0"
  }
}
